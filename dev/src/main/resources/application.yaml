server:
  port: 8080
  servlet:
    context-path: /dev

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/db001"
    username: postgres
    password: Admin@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
